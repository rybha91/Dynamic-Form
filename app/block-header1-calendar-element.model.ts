export class BlockHeader1CalendarElement {
    header: string;
    subElements: [
            {
                appointmentId: string;
                date: string;
                time: string;
                purpose: string;
                status: string;
                name: string;
                // headingLabel: string;
                // subHeadingLabel: string;
                navPath: string;
            }
        ]
}